# 예제 07 - Swing + Thread

## 설명
EDT(`SwingUtilities.invokeLater`)와 백그라운드 스레드를 함께 사용하는 예제입니다.

## 실행 방법
```bash
./gradlew :ex07_swing_thread:run
```

## 빌드(컴파일) 방법
```bash
./gradlew :ex07_swing_thread:build
```
